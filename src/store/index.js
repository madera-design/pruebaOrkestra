import { createStore } from 'vuex';

const store = createStore({
  state: {
    // Aquí se definen las propiedades del estado inicial
  },
  mutations: {
    // Aquí se definen las mutaciones para modificar el estado
  },
  actions: {
    // Aquí se definen las acciones que pueden llamar a las mutaciones
  },
  getters: {
    // Aquí se definen los getters para obtener valores del estado
  }
});

export default store;